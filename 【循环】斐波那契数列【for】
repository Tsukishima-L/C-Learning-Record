#include <stdio.h>

int main() {
    int n, i;
    long long a = 1, b = 1, c;
    scanf("%d", &n);
    for (i = 1; i <= n; i++) {
        printf("%lld", a);
        if (i != n) {
            printf(" ");
        }
        c = a + b;
        a = b;
        b = c;
    }
    return 0;
}
